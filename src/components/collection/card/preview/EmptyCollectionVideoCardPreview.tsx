import React from 'react';

const EmptyCollectionCardVideoPreview = React.memo(() => (
  <section className="collection-card-video-container">
    <section className="collection-card-video-container__inner collection-card-video-container--blue" />
  </section>
));

export default EmptyCollectionCardVideoPreview;
