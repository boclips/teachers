apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: teachers
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: REPLACEME
    http:
      paths:
      - path: /
        backend:
          servicePort: http
          serviceName: teachers
